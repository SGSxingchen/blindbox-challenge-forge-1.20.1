# P1 核心 MVP 验收记录

验收提交：`f0daf4eb7b9c0294d82f9b02c4b2ff7b7f5dec45`

P1 已完成盲盒、打包道具、全局共享奖池、事务 schema v2、PACK/OPEN 持久收据、登录幂等恢复、奖项保留与竞争控制、独立 ciTest 探针和强制回归汇总。

## Hosted Runner 门禁

- 质量与构建：run `31068700840`，成功。
- 真实 Forge 专服：run `31068700865`，成功。
- 生命周期强杀恢复：run `31068700864`，成功。
- 真实 Xvfb 单客户端：run `31068700838`，成功。
- 真实双客户端联机与业务回归：run `31068700843`，成功。
- P1 强制回归汇总：run `31068959525`，成功。

## 已验证场景

- 空奖池、正常打包/开盒、唯一名称/耐久/附魔/自定义 NBT 资产守恒。
- 满背包原子失败、菜单关闭与重放拒绝、伪造 containerId/nonce、陈旧指纹。
- 两玩家竞争最后 bundle、失败方盲盒保留、重复打包/开盒与 64 次突发重放不复制不吞物。
- 松手、换手、死亡、掉线重连后的开盒状态与减速修饰清理。
- `save-all flush → SIGKILL(-9) → 同世界重启` 后 PACK/OPEN bundle、事务收据、payload 与保留关系保持一致。
- 正式 Jar 不含 ciTest 探针；动态测试只在 GitHub Hosted Runner 执行。

## 明确边界

未执行 `save-all flush` 的真实掉电窗口不由本轮结果外推。证据冲突会稳定进入 `MANUAL_REVIEW`，不会猜测性补发、删除或生成公共掉落。该边界不阻塞 P2 基础物品族开发。
