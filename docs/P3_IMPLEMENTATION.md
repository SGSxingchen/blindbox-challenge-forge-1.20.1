# P3 持久能力、抱枕投掷与返航剪刀实施记录

> 阶段工单：[Issue #3](https://github.com/SGSxingchen/blindbox-challenge-forge-1.20.1/issues/3)。P3 以已归档验收的 P2（50/50，验收提交 `179c207723cbe59aa2ca03548d6870574c5a0b41`）为基线；第一批代码尚未提交 Hosted Runner，不能把静态实现写成动态验收通过。

## 阶段范围

P3 固定覆盖以下 **6 项**。第一批已完成 009、033 的代码、资源、网络方向门禁和隔离探针，待同一提交的六门禁验证；其余 4 项尚未实现：

|编号|对象|本阶段最低交付|当前状态|
|---|---|---|---|
|008|石墩子抱枕|可放置、可坐、对空气长按蓄力投掷|未开始|
|009|易筋经|首次学习后永久生命、攻击与一次空中跳能力|第一批代码完成，待门禁|
|011|高效养猪技术|10 格球形范围内的服务端催生繁殖|未开始|
|016|钻石抱枕|可放置、可坐、对空气长按蓄力投掷|未开始|
|033|路障头饰|铁头盔等值的头部装备|第一批代码完成，待门禁|
|045|返航剪刀|三叉戟基线的投掷、命中、返航和完整物品归还|未开始|

001、002 已在 P2 完成，不作为 P3 的“变形物”重复计数。TAC 玩具 AK、魔女服、金色手枪、乐魂及编号 012 继续排除。信件、死亡笔记、任意门、安全落点、发条小黄鸡和在线音频属于 P4；最终专属模型、动画、资源审查与正式发布属于 P5。

## 已锁定的实施边界

### 服务端权威与持久状态

* 只新增一个挂在玩家身上的 Capability。易筋经的已学习状态和二段跳个人冷却到期刻写入其 NBT；本次滞空的二段跳使用标记只由服务端运行期维护并在落地重置，不在 Capability 中保存玩家、世界或实体的强引用。
* 首次学习、物品消耗、固定 UUID 的最大生命与攻击属性对账、空中跳次数和速度均由逻辑服务端决定。重复使用不得重复叠加属性或重复消耗物品。
* 玩家死亡克隆、登录、换维、开始被跟踪和状态变更后均须同步。客户端仅缓存已同步的能力数据并处理按键/显示，不能自行授予能力、计算冷却或施加速度。
* 二段跳只能由受方向限制的 C2S 意图包请求。服务端须重新核验玩家存活、非睡眠、已学习、非旁观、非原版飞行、腾空状态、一次腾空尚未使用及冷却，再施加默认初速度 `0.42` 和保守的 5 tick 个人冷却；落地状态只由服务端重置。

### 交互、实体与物品守恒

* 011 的猪扫描、范围判定、冷却、扫描上限和繁殖触发全部在逻辑服务端执行。默认范围为以使用者为中心的 10 格球形；冷却和扫描上限以服务器配置为准，不能让客户端提供目标列表。
* 008、016 共用抱枕投掷实体与座位实体，物品放置、上座、下座、蓄力、实体生成、命中伤害与回收均由服务端处理。两种抱枕以受同步的变体数据区分，不能在客户端生成实体或以客户端标记决定命中。
* 045 以原版三叉戟的服务端投掷/返航语义为基线。投出的完整 `ItemStack` 必须在实体 NBT 中可恢复；返航、拾取和满包兜底都不得复制或静默丢失物品。
* 033 保持铁头盔等值的耐久和护甲语义。P3 交付最低可运行盔甲层；专属路障模型与精细动画仍留 P5，不能提前宣称完成。

### 双端隔离与资源

* 公共生产代码和专服加载路径不得导入客户端类。按键、客户端能力缓存、座位/飞行显示和模型渲染放入客户端隔离入口；C2S/S2C 包各自显式声明方向。
* P3 新增的图标、方块、实体、盔甲层、模型和双语文本均须可运行且为原创资源。不得添加原始照片、压缩素材、第三方成品贴图或在线音频文件。
* P3 开始引入 S2C 后，质量门禁必须按包逐一断言方向：打包与二段跳是 C2S，能力同步是 S2C；不得保留“全局禁止 S2C”的过时断言。

## 批次计划

|批次|内容|完成判据|
|---|---|---|
|第一批|009 玩家 Capability、固定 UUID 属性对账、克隆/登录/换维/跟踪同步、二段跳 C2S；033 路障头饰|代码、原创资源、网络方向门禁和真实专服探针已完成，待六门禁|
|第二批|011 高效养猪技术|范围、扫描上限、冷却和繁殖由服务端断言|
|第三批|008、016 抱枕方块、座位与共用投掷实体|两客户端实际观察到放置、坐姿、飞行和命中；物品守恒|
|第四批|045 返航剪刀及 P3 汇总回归|投掷、命中、返航、满包兜底、完整 NBT 归还及 P1/P2 回归全绿|

每个实质批次都须同步更新本文件、[P3 验收矩阵](P3_ACCEPTANCE.md) 和 Issue #3；只暂存本批预期文件。动态 Gradle、Forge 与 Minecraft 验证只允许 GitHub Hosted Runner 执行，本机仅进行文本、路径、资源格式与差异静态检查。

## 验收方式与真实未覆盖边界

隔离探针只能存在于 `src/ciTest` 及独立 ciTest Jar，正式 Jar 不得含 `cn/blindboxchallenge/citest/`。探针必须从真实服务端状态读取断言，客户端观察标记只能在真实观察到坐姿、投掷实体和返航之后产生；禁止预写标记、`continue-on-error`、删除失败场景或用编译成功代替行为覆盖。两客户端探针还必须保留 P1 canonical 资产守恒和 P2 已有业务回归。

当前 P3 为 **2/6 静态实现、0/6 阶段验收**。第一批隔离探针从真实专服状态断言首次/重复学习、属性 UUID、未学习拒绝、一次腾空限制、落地重置、5 tick 个人冷却、服务端速度及铁头盔等值属性；现有真实双客户端断线重连夹具还会在断开前学习、重连后复核 Capability NBT 和固定 UUID 属性，再清理 P3 状态。真实客户端按键/C2S 传输、死亡克隆、换维、跟踪与强杀恢复的 P3 专项夹具仍待补齐，不能用 P1/P2 的绿灯或直接服务调用外推。P1 已知的“未执行 `save-all flush` 的真实掉电窗口”仍未覆盖，不能外推为已解决；其余 P4/P5 范围同样不属于 P3 结论。完整门禁状态与后续真实运行编号见 [P3 验收矩阵](P3_ACCEPTANCE.md)。
