# P2 基础物品族实施记录

## 第一批（2026-08-06）

已实现首批 9 个需求对象：

- 003 盐酸肾上腺素：24 tick 使用，服务端施加 30 秒速度 II、力量 II、生命恢复 IV，并立即给予饱和效果；默认最大堆叠 16。
- 006 奶龙老鼠干：主手或副手触发与不死图腾一致的保命药效和动画，虚空等绕过无敌伤害不触发。
- 014 一米长螺丝刀：铁剑伤害、攻速、耐久，并增加 1 格实体攻击距离。
- 019 镐锄一体：铁锄交互，同时具铁镐 ToolAction、采掘速度和掉落等级判定。
- 024 打火机：沿用原版打火石交互与 64 点耐久。
- 021 黑松露火腿苏打饼干、042 太阳糖：曲奇数据，2 饥饿值、0.1 饱和系数。
- 030 呀土豆零食：牛排数据，8 饥饿值、0.8 饱和系数。
- 032 军粮包：20 饥饿值、1.0 饱和系数。

全部资源使用小写路径、中英文语言、原创 16×16 可运行占位贴图和标准 item model。动态 Forge 验证只在 GitHub Hosted Runner 执行；当前提交先由质量、专服、单客户端、双客户端、强杀恢复和汇总门禁验证，随后补 P2 专用业务探针。

## 首批动态业务验收

真实双客户端联机套件新增服务端权威断言：四类食物数据、肾上腺素消耗与药效等级/时长、奶龙老鼠干的图腾式保命与药效、螺丝刀攻击距离和铁剑耐久、镐锄的铁镐/铁锄动作与钻石矿掉落等级、打火机 64 点耐久。断言由隔离 `ciTest` 探针执行，不进入正式 Jar。

## 第二批（2026-08-06）

新增 7 个无持久状态的低风险需求对象：

- 005-B 仙女棒：木剑基线，主手提供 +2 攻击击退，语义等同“击退 II”。
- 043 鲨匕抱枕：完整沿用石剑伤害、攻速和耐久。
- 013 风火轮玩具车、017 百万英镑、023 小升初模拟卷数学品、034 王立新徽章、037-A 流动黑旗：按原需求注册为无效果收藏品，不擅自增加放置、骑乘或交互。

全部补齐中英文名称、标准 item model 和原创 16×16 占位贴图；未使用或上传原始照片。当前提交已通过六条 Hosted Runner 门禁。后续增量已补服务端权威断言：仙女棒必须保持木剑耐久、伤害、攻速并提供 +2 攻击击退；鲨匕抱枕必须保持石剑耐久、伤害和攻速。断言继续由隔离 `ciTest` 探针在真实双客户端专服中执行。

## 第三批（2026-08-06）

新增 6 个无额外状态的基础食物：022 大白兔奶糖采用曲奇数据；035-A 深海小鱼采用 2/0.1；035-B 火腿肠采用 4/0.3；035-C 鹌鹑蛋采用 2/0.2；040 绿豆浆采用 4/0.3；044 牛肉粒采用 6/0.6。未指定数值按保守默认基线实现，后续可配置化时保持注册名不变。全部使用原创抽象像素图，不复刻包装、角色或商标。真实双客户端专服的 `run_p2_business` 同步断言六项食物数据。


## 第四批（2026-08-06）

新增 7 个基础食物：046-A 油板栗（4/0.3）、046-B 风吹饼（4/0.3）、047-A 糖醋火鸡面（8/0.7）、047-D 麻酱米线（8/0.6）、048 呀土豆（6/0.5）、049 黑松露火腿苏打饼干（2/0.1）、050 魔法士干脆面（6/0.5）。未指定数值继续采用保守默认基线，注册名保持稳定。全部使用原创抽象 16×16 像素贴图，不复刻商品包装、商标或角色。隔离 `ciTest` 的真实双客户端服务端业务套件同步断言全部食物数据。

## 第五批（2026-08-06）

新增 031 卡祖笛：右键由逻辑服务端向玩家声源位置广播原版长笛音色，物品不消耗且不可堆叠；每次成功吹奏施加 20 tick 冷却，冷却期间重复请求明确拒绝，避免多人服务器音效刷屏。补齐双语、标准模型和原创 16×16 黄铜卡祖笛贴图。真实双客户端专服的 `run_p2_business` 断言成功交互、服务端冷却、重复使用拒绝、物品数量与堆叠上限。

## 第六批（2026-08-06）

新增 8 个装备与常规物品：

- 004 美甲：主、副手各增加攻击力 1 和实体攻击距离 2；四个属性修饰器均使用独立 UUID，因此双手可以叠加而不会相互覆盖。
- 005-A 粉色蝴蝶翅膀：直接使用原版鞘翅的胸甲槽、耐久 432、装备与飞行语义。
- 007-B 玩具小刀：木质耐久的保守低伤害近战物品。
- 015 链锯剑：继承石斧的耐久、攻击、攻速和斧类交互，并增加实体攻击距离 2。
- 037-C 蛋仔眼罩：头部装备；仅由逻辑服务端的装备变化事件添加、清理其无限失明，且不删除外部短时失明。
- 039-A 文绪立牌：主手或副手的图腾式保命语义，与奶龙老鼠干共用服务端死亡保护路径。
- 047-E 猫玩偶：无效果收藏品，不擅自增加放置或交互。
- 052 口罩：头部装备。

本批补齐中英文名称、标准物品模型和原创 16×16 RGBA 像素贴图。真实双客户端专服的隔离 `ciTest` 断言美甲双手 UUID/属性、鞘翅耐久与胸甲槽、木质小刀、石斧链锯剑、眼罩服务端失明、两种头部装备、立牌图腾语义和猫玩偶无效果；探针在 `finally` 中恢复主副手、头盔、生命和药效，不污染 P1 的 canonical 资产守恒断言。翅膀与头饰暂使用原版可运行装备外观，专属背部/头部模型精修保留给 P5，不能宣称已经完成该美术层。

## 第七批（2026-08-06）

新增 4 个容器与照明物品：

- 025 70cm 泡澡桶：可收取水源或岩浆源；容器状态写在本物品 NBT 中，收取岩浆才损耗 1 点耐久，总耐久 10。倒出时复用原版桶的安全放置语义而不将本物品替换成原版桶。
- 026 荧光棒：使用原版火把的站立/墙面放置与照明语义。
- 028 BML 应援棒一对：一个注册物品，对应站立/墙面放置；右键只能由逻辑服务端切换持久 `LIT` 方块状态，关闭时不发光、开启时 14 级光照。
- 046-F 纸杯：只允许收取和倒出水；拒绝岩浆，容器状态保留在本物品 NBT 中。

新增的墙面火把是站立物的技术伴生方块，不是额外命名需求物品。所有方块均有 blockstate、方块/物品模型、战利品表、中英文语言和原创 16×16 RGBA 贴图。隔离 `ciTest` 在两个真实客户端在线的专服中真实调用生产右键入口，断言泡澡桶取水/倒水/取岩浆耐久、纸杯拒岩浆/取水，以及应援棒只在服务端写入 `LIT`；夹具结束后恢复世界方块与玩家状态，避免影响 P1 canonical 资产断言。

## 第八批（2026-08-06）

新增 2 个可变基础工具：

- 001 黑武士伸缩刀：木剑耐久、攻击和攻速基线。右键仅由逻辑服务端切换单个 ItemStack 的 `Extended` NBT；伸出状态命中后由服务端按保守固定 **20%** 概率收缩。
- 002 紫色玩具钻石镐/剑：右键仅由逻辑服务端切换 `PickaxeForm` NBT。默认镐形态具木镐 ToolAction、速度与掉落等级，剑形态具木剑近战/横扫语义。Forge 的 ItemStack 属性事件按当前 NBT 给主手提供真实木镐或木剑属性，避免把两个形态伪装成同一套属性。

客户端仅在 `ClientModEvents` 注册只读 NBT 模型谓词，公共生产代码不导入客户端类。补齐中英文、4 个形态模型及 4 张原创 16×16 RGBA 贴图。真实专服 `ciTest` 走生产右键入口、断言 NBT/ToolAction/速度/掉落等级/属性；伸缩刀概率分支使用生产共用的确定性滚点边界断言，并保留真实命中耐久断言，避免概率性 CI。容器夹具的岩浆源与真实倒水区物理隔离，避免原版水岩浆反应伪造失败。

## 第九批（2026-08-06）

完成 P2 最后 6 项基础物品，至此本阶段代码实施为 **50/50**：

- 020 伏特加：饮用 24 tick 后仅由逻辑服务端写入 600 tick（30 秒）原版恶心 I；客户端只按已同步的原版效果呈现视觉，不保存或决定醉酒计时。默认最大堆叠 16。
- 027 头戴式耳机：右键仅由逻辑服务端广播原版 `MUSIC_DISC_CAT` 声音，物品不消耗、不可堆叠，并写入 20 tick 冷却；冷却中重复请求由服务端拒绝。此物品不接收、保存、下载或解码 URL/在线音频。
- 029 安全出口牌子盾牌：沿用原版盾牌使用语义，耐久固定为 5 且不可修复。仅在逻辑服务端收到原版“成功格挡”事件后，向直接的 `LivingEntity` 攻击者造成被格挡伤害 50% 的荆棘反伤；反伤调用栈用线程内防递归标记保护。
- 039-B 决策币：右键的随机掷币、效果写入与消耗均由服务端完成；正面给予力量 II 200 tick（10 秒），反面仅清除使用者当时的有益状态效果。默认最大堆叠 16。
- 046-E 生日蜡烛：长按 32 tick 完成许愿后，服务端从速度 I、力量 I、抗性 I、生命恢复 I 中随机给予一种 600 tick（30 秒）效果，并写入 400 tick（20 秒）冷却；物品不可堆叠。
- 046-G 彩虹圈：松开使用时仅由逻辑服务端处理蓄力。少于 10 tick 不触发；10–40 tick 按线性曲线把自身竖直速度设为 0.45–0.90，超过 40 tick 封顶。默认不提供落伤免疫或额外冷却，物品不可堆叠。

本批同样补齐稳定小写注册名、中英文语言键、标准物品模型和原创 16×16 RGBA 像素贴图。隔离 `ciTest` 在两名真实客户端在线的专服中走生产使用/完成使用/格挡/松手入口，断言伏特加消耗与服务端恶心、耳机冷却和重复拒绝、盾牌耐久与反伤及防递归释放、决策币真实随机入口和两面确定性业务分支、生日蜡烛冷却及全部候选分支、彩虹圈的服务端速度边界。探针在 `finally` 中恢复双方手持物、药效、生命、速度、受伤同步标志及冷却，不把测试状态带入 P1 资产守恒断言。

### 本批保守默认与真实边界

- 020 使用原版已同步的恶心视觉，不另做自定义模糊/扭曲着色器；效果时长仍以服务端为准。
- 027 按既定分期只播放原版声音事件；在线 URL 的校验、下载、缓存、解码、全服同步和一次播放策略属于 P4 的 047-B 八音盒，不能据此宣称耳机已有在线音频能力。
- 029 只反射成功格挡时的直接活体攻击者；投射物、其他间接伤害及攻击者不为活体时保持原版行为。反伤的伤害源采用原版荆棘语义。
- 039-B 的反面只清除有益状态效果；中毒、凋零等负面效果保留，避免把“清除 buff”误实现为治疗负面状态。
- 046-E 的四种正面效果和 046-G 的蓄力区间/速度是未给定数值时采用的保守默认值；尚未配置化。彩虹圈也不免疫落伤。
