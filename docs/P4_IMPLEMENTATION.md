# P4 交互、传送与在线音频实施记录

> 阶段工单：[Issue #4](https://github.com/SGSxingchen/blindbox-challenge-forge-1.20.1/issues/4)。P3 已归档；本文件从 P4 正式开工起记录实际提交与动态证据，不能把准备结论写成实现完成。

## 阶段范围与状态

|对象|最低交付|状态|
|---|---|---|
|CORE-03 信件|受限纯文本阅读、服务端会话编辑、NBT 修订|第一批静态实现，待 Hosted Runner 验证|
|038 死亡笔记|在线目标解析、受控延迟与服务端执行|第一批静态实现，待 Hosted Runner 验证|
|037-B 任意门 + CORE-04 安全落点|双向配对、关联校验和安全传送|第二批静态实现，待 Hosted Runner 验证|
|046-D 发条小黄鸡|可恢复的倒计时实体与原版 TNT 语义爆炸|第三批静态实现，待 Hosted Runner 验证|
|047-B 木质手工八音盒|安全 HTTPS URL、全服一次播放、客户端 OGG/MP3 下载缓存解码|第四批静态实现，待 Hosted Runner 验证|

027 耳机保持原版音乐；TAC、魔女服、乐魂、原始照片与压缩素材不做。详细裁决见 [P4 准备记录](P4_PREPARATION.md)。

## 实施原则

* 注册、中文/英语文本、原创可运行资源、服务端权威、客户端隔离、业务探针和本文档同批交付。
* 信件、死亡笔记和八音盒的提交均使用服务端会话，重新核验玩家、手持实例、槽位、修订、长度、距离、权限和目标；不执行拼接命令。
* 任意门不强加载区块；安全落点是独立 `Block` 与 `BlockItem`，只在关联、区块、维度和碰撞均通过时传送。
* 在线音频仅允许安全 HTTPS URL；服务端不下载或转发，播放事件仅发送给开始时当前在线的全服玩家。MP3 必须是许可证兼容的纯客户端解码实现，专服绝不能加载音频客户端类。
* ciTest 仅存在于 `src/ciTest` 与独立 Jar，不能预写 marker、跳过失败、删除场景或放宽生产校验。

## 当前批次

第一批已写入信件与死亡笔记的生产链路，尚未取得动态结论：

* 信件普通右键只发送受过滤的只读 S2C 正文；潜行右键创建含容器、会话、手持槽位、实例 UUID 和修订号的服务端菜单。提交只接受统一换行的纯文本，拒绝控制字符与 `§`，并按服务器配置检查最多 512 Unicode 码点、16 行；成功后才递增 NBT 修订。
* 死亡笔记菜单以相同的实例与修订边界校验提交时在线的玩家名，并立即固化目标 UUID 与到期游戏刻到主世界 `DeathNoteSavedData`；到期仅按 UUID 查仍在线的目标，以绕过无敌的原版伤害结算，绝不拼接或执行命令。
* 本批新增原创 16×16 像素图标、物品模型、中文/英文文本和客户端隔离 Screen；质量工作流同步断言三种新网络包方向及 C2S 手持实例复验。
* ciTest 新增真实双客户端路径：Alice 以生产 `KeyMapping` 右键、潜行右键和 Screen 控件进入读信、编辑和死亡笔记；Bob 仅在真实死亡界面出现后写 marker。负例覆盖换手、伪造容器/实例/修订、一次性会话重放、控制字符、码点/行数超限和离线目标；生命周期工作流另保存并核对尚未到期的死亡笔记 UUID/到期刻。

死亡笔记的已落盘排程可跨 `save-all flush → SIGKILL → 同世界重启` 保留；但世界 `SavedData` 与玩家伤害不存在数据库原子提交，**到期刻恰好强杀的极窄窗口不能同时证明不漏结算和绝不重复结算**。P4 最终验收必须如实保留此边界，不能外推为严格恰一次。后续批次再分别处理任意门/安全落点、小黄鸡和八音盒；每个批次完成后同步本文件、[P4 验收矩阵](P4_ACCEPTANCE.md)、`AGENTS.md` 与 Issue #4。

第二批已开始写入任意门与独立安全落点的最小服务端链路，尚未取得动态结论：门本体是无碰撞 `Block + BlockEntity`，只保存门 UUID、反向门 `GlobalPos` 和目标安全点 `GlobalPos`；安全落点是带 `BlockItem` 的独立完整方块。玩家潜行右键先选第一门、再配对第二门，双方各自必须有且只有一个下方或水平相邻的安全落点。进入门体只由服务端校验门反链、双方安全点、维度、已加载区块、相邻安全点、站立碰撞、液体、构建高度和世界边界后传送；任一失败保持原地，绝不强加载区块。若安全点位于门下，玩家抵达门格后保持免疫直到真实走出该格，避免冷却期后反向回跳。伙伴区块未加载时，以主世界 `DoorInvalidationSavedData` 保存伙伴门 UUID 失效回执，并在该门下次交互或进入时惰性清链；不为清理关系强加载区块。ciTest 已扩展为正式 `Block#use` 配对、`entityInside` 成功传送、下方安全点即时反向拒绝、拆两侧安全点与两侧门的双向清链、自指、未加载目标不强加载以及出口碰撞拒绝；跨维真实双客户端观察和强杀后真实进入门体仍待本批 Hosted Runner 后补全。

第三批已开始写入 046-D 发条小黄鸡的最小服务端链路，尚未取得动态结论：右键只在逻辑服务端经正式 `Item#use` 创建独立 `PrimedTnt` 派生实体，成功加入世界后才扣物。实体 NBT 保存主人 UUID、武装游戏刻、Fuse 和爆炸威力；武装时冻结当前服务器配置，避免之后改配置悄然修改已放出的倒计时。到期仅由父类 Fuse 归零路径触发一次，使用 `Level.ExplosionInteraction.TNT` 与默认强度 8；客户端只同步并渲染实体，绝不计算 Fuse 或爆炸。ciTest 已新增两真实客户端观察同一实体 UUID/Fuse、服务端反查、默认 1200 tick 倒计时与单次 TNT 来源爆炸断言；生命周期工作流已准备核验同一 UUID、主人、武装刻、Fuse、威力和门方块实体字段跨 `save-all flush → SIGKILL` 保存。上述新增探针尚未经 Hosted Runner 编译和运行，不得写为已验收。

第四批已开始写入 047-B 木质手工八音盒的最小完整链路，尚未取得动态结论：方块实体只持久保存实例 UUID、已规范化的 HTTPS URL 和修订号，不保存正在播放的事件，因此重登绝不补播。潜行右键或空配置右键打开服务端受控菜单；C2S 提交重新核验菜单、方块位置、距离、实例和修订，拒绝非 HTTPS、认证信息、片段、IP 字面量、localhost 与非 443 端口。普通右键只向**触发时已在线的玩家**发送一次 UUID 事件；服务端从不下载、缓存、解码或转发音频字节。客户端对每个重定向重新规范化 URL、DNS 解析并拒绝私网/保留地址；已通过校验的公网 IP 被固定到实际 TLS 连接，同时保留 SNI 与 HTTPS 主机名校验，避免“预解析后又按域名连接”的 DNS 重绑定漏洞。下载禁用自动重定向、代理、压缩传输、Cookie/认证请求头，限制 3 跳、10 秒、16 MiB，验证 Content-Type 与 OGG/MP3 文件头；成功后按 URL 与内容 SHA-256 原子落盘并以 64 MiB LRU 清理。OGG 使用原版 `OggAudioStream`，MP3 使用仅客户端 JLayer 1.0.1（LGPL-2.1、独立 Jar-in-Jar、不重定位；完整许可证与 NOTICE 已随源树保存）解码 16-bit PCM，再经 Forge 的 `SoundInstance#getStream` 公开扩展点交给原版 SoundEngine。不得把这批静态实现写为安全或播放验收通过；真实 HTTPS、OGG/MP3、缓存、失败和新登录不补播探针仍须在 Hosted Runner 完成。
