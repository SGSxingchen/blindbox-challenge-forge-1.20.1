# P4 交互、传送与在线音频实施记录

> 阶段工单：[Issue #4](https://github.com/SGSxingchen/blindbox-challenge-forge-1.20.1/issues/4)。P3 已归档；本文件从 P4 正式开工起记录实际提交与动态证据，不能把准备结论写成实现完成。

## 阶段范围与状态

|对象|最低交付|状态|
|---|---|---|
|CORE-03 信件|受限纯文本阅读、服务端会话编辑、NBT 修订|第一批静态实现，待 Hosted Runner 验证|
|038 死亡笔记|在线目标解析、受控延迟与服务端执行|第一批静态实现，待 Hosted Runner 验证|
|037-B 任意门 + CORE-04 安全落点|双向配对、关联校验和安全传送|第二批静态实现，待 Hosted Runner 验证|
|046-D 发条小黄鸡|可恢复的倒计时实体与原版 TNT 语义爆炸|第三批静态实现，待 Hosted Runner 验证|
|047-B 木质手工八音盒|安全 HTTPS URL、全服一次播放、客户端 OGG/MP3 下载缓存解码|第四批静态实现，待 Hosted Runner 验证|

027 耳机保持原版音乐；TAC、魔女服、乐魂、原始照片与压缩素材不做。详细裁决见 [P4 准备记录](P4_PREPARATION.md)。

## 实施原则

* 注册、中文/英语文本、原创可运行资源、服务端权威、客户端隔离、业务探针和本文档同批交付。
* 信件、死亡笔记和八音盒的提交均使用服务端会话，重新核验玩家、手持实例、槽位、修订、长度、距离、权限和目标；不执行拼接命令。
* 任意门不强加载区块；安全落点是独立 `Block` 与 `BlockItem`，只在关联、区块、维度和碰撞均通过时传送。
* 在线音频仅允许安全 HTTPS URL；服务端不下载或转发，播放事件仅发送给开始时当前在线的全服玩家。MP3 必须是许可证兼容的纯客户端解码实现，专服绝不能加载音频客户端类。
* ciTest 仅存在于 `src/ciTest` 与独立 Jar，不能预写 marker、跳过失败、删除场景或放宽生产校验；其类只能声明在独立的 `cn.blindboxchallenge.citest` 包树中，可引用生产公开类型但绝不能与正式模组共享 Java 包，否则 Forge 模块层会拒绝启动。八音盒生产声源只额外公开事件 UUID、格式、缓存命中和声源位置等只读元数据，供独立探针包装生产 `SoundInstance` 后观察真实 PCM；探针不获得下载、解码或播放控制入口。

## 当前批次

第一批已写入信件与死亡笔记的生产链路，尚未取得动态结论：

* 信件普通右键只发送受过滤的只读 S2C 正文；潜行右键创建含容器、会话、手持槽位、实例 UUID 和修订号的服务端菜单。提交只接受统一换行的纯文本，拒绝控制字符与 `§`，并按服务器配置检查最多 512 Unicode 码点、16 行；成功后才递增 NBT 修订。
* 死亡笔记菜单以相同的实例与修订边界校验提交时在线的玩家名，并立即固化目标 UUID 与到期游戏刻到主世界 `DeathNoteSavedData`；到期仅按 UUID 查仍在线的目标，以绕过无敌的原版伤害结算，绝不拼接或执行命令。
* 本批新增原创 16×16 像素图标、物品模型、中文/英文文本和客户端隔离 Screen；质量工作流同步断言三种新网络包方向及 C2S 手持实例复验。
* ciTest 新增真实双客户端路径：Alice 以生产 `KeyMapping` 右键、潜行右键和 Screen 控件进入读信、编辑和死亡笔记；Bob 仅在真实死亡界面出现后写 marker。负例覆盖换手、伪造容器/实例/修订、一次性会话重放、控制字符、码点/行数超限和离线目标；生命周期工作流另保存并核对尚未到期的死亡笔记 UUID/到期刻。

死亡笔记的已落盘排程可跨 `save-all flush → SIGKILL → 同世界重启` 保留；但世界 `SavedData` 与玩家伤害不存在数据库原子提交，**到期刻恰好强杀的极窄窗口不能同时证明不漏结算和绝不重复结算**。P4 最终验收必须如实保留此边界，不能外推为严格恰一次。后续批次再分别处理任意门/安全落点、小黄鸡和八音盒；每个批次完成后同步本文件、[P4 验收矩阵](P4_ACCEPTANCE.md)、`AGENTS.md` 与 Issue #4。

第二批已开始写入任意门与独立安全落点的最小服务端链路，尚未取得动态结论：门本体是无碰撞 `Block + BlockEntity`，只保存门 UUID、反向门 `GlobalPos` 和目标安全点 `GlobalPos`；安全落点是带 `BlockItem` 的独立完整方块。玩家潜行右键先选第一门、再配对第二门，双方各自必须有且只有一个下方或水平相邻的安全落点。进入门体只由服务端校验门反链、双方安全点、维度、已加载区块、相邻安全点、站立碰撞、液体、构建高度和世界边界后传送；任一失败保持原地，绝不强加载区块。为隔离原版在 `entityInside` 后继续处理触发它的旧位置包，所有门请求都只先记下源门，统一在该服务器刻 `END` 按玩家 UUID、仍与源门方块相交的碰撞盒和上述全部事实重新校验后才执行；排队本身不是授权或成功。同维使用原版位置包；跨维必须由 Forge `changeDimension` 与不创建门户的精确 `ITeleporter` 迁移服务端世界、客户端维度包和跟踪状态，随后服务端显式清零并同步进入源门前的速度，确保已验证的目的安全格确为站立格，而不是让残余惯性把玩家带离该格。若安全点位于门下，玩家抵达门格后保持免疫直到真实走出该格，避免冷却期后反向回跳；目标门免疫在真正迁移前预留，失败才精确回滚，防止迁移回调重入。伙伴区块未加载时，以主世界 `DoorInvalidationSavedData` 保存伙伴门 UUID 失效回执，并在该门下次交互或进入时惰性清链；不为清理关系强加载区块。ciTest 已扩展为正式 `Block#use` 配对、`entityInside` 排队后在生产 `ServerTick.END` 消费、下方安全点即时反向拒绝、拆两侧安全点与两侧门的双向清链、自指、未加载目标不强加载以及出口碰撞拒绝。

任意门强杀恢复的跨维双客户端探针也已落盘，仍待 Hosted Runner 动态结论：杀前在主世界源门、下界目标门通过生产方块实体写入双向 UUID、伙伴/安全点 `GlobalPos`，并原子保存 `p4-door-recovery-before.properties`；它只是杀前持久字段证据，不是成功 marker。同一次真实双客户端会话先执行 `save-all flush`，再 `SIGKILL` 并同世界重启；Bob 通过不创建门户的 Forge 完整玩家跨维迁移抵达下界出口附近，以正常玩家语义加载目标区块，门逻辑本身不强加载。P3 恢复后的 Alice 同样先由完整迁移回到主世界源门，且夹具立即复验其维度、坐标和速度；这只是定位夹具，之后唯一允许 Alice 到达下界的路径仍是生产门逻辑。跨维后夹具一律按 UUID 重新从玩家列表取得当前在线对象，清理也按原维度、原坐标走完整迁移，不能把旧实体引用误当作传送结果。重启后服务端严格比对杀前 UUID、双方反链、两端 `GlobalPos` 与安全点，不重新配对；仅在此后，Alice 才通过生产 `KeyMapping.keyUp` 真实走入源门，并在本地首次进入源门格时松键，避免夹具的持续输入越过跨维同步首帧；若门未传送，既有超时仍会失败。跨维门由玩家进入的同一原版移动包触发，原版只会在客户端确认 teleport id 后将服务端位置最终复位为等待中的目标坐标；探针因此仅在 `isChangingDimension()` 清除后严格核验位置、速度、反链和客户端 marker，确认等待本身不是成功。Alice 到达下界目标站立格后写到达 marker；Bob 仅在真实同步到远程 Alice、目标门和安全落点后写观察 marker；服务端再核验两份 marker、Alice 实际位置、归零后的速度和持久门字段。工作流启动时只可删除旧 marker；杀前夹具存在时客户端完全不走动，重启后阶段旗标只允许开始观察，绝不代表成功。超时、旧 marker 或任一字段不符均失败。

第三批已开始写入 046-D 发条小黄鸡的最小服务端链路，尚未取得动态结论：右键只在逻辑服务端经正式 `Item#use` 创建独立 `PrimedTnt` 派生实体，成功加入世界后才扣物。实体 NBT 保存主人 UUID、武装游戏刻、Fuse 和爆炸威力；武装时冻结当前服务器配置，避免之后改配置悄然修改已放出的倒计时。到期仅由父类 Fuse 归零路径触发一次，使用 `Level.ExplosionInteraction.TNT` 与默认强度 8；客户端只同步并渲染实体，绝不计算 Fuse 或爆炸。ciTest 已新增两真实客户端观察同一实体 UUID/Fuse、服务端反查、默认 1200 tick 倒计时与单次 TNT 来源爆炸断言；生命周期工作流使用独立石质支撑面固定恢复夹具，避免原版 TNT 重力导致位置变化被误判为 NBT 恢复失败，并核验同一 UUID、主人、武装刻、Fuse、威力和门方块实体字段跨 `save-all flush → SIGKILL` 保存。上述新增探针尚未经 Hosted Runner编译和运行，不得写为已验收。

第四批已开始写入 047-B 木质手工八音盒的最小完整链路，尚未取得动态结论：方块实体只持久保存实例 UUID、已规范化的 HTTPS URL 和修订号，不保存正在播放的事件，因此重登绝不补播。潜行右键或空配置右键打开服务端受控菜单；C2S 提交重新核验菜单、方块位置、距离、实例和修订，拒绝非 HTTPS、认证信息、片段、IP 字面量、localhost 与非 443 端口。普通右键只向**触发时已在线的玩家**发送一次 UUID 事件；服务端从不下载、缓存、解码或转发音频字节。客户端对每个重定向重新规范化 URL、DNS 解析并拒绝私网/保留地址；已通过校验的公网 IP 被固定到实际 TLS 连接，同时保留 SNI 与 HTTPS 主机名校验，避免“预解析后又按域名连接”的 DNS 重绑定漏洞。下载禁用自动重定向、代理、压缩传输、Cookie/认证请求头，限制 3 跳、10 秒、16 MiB，验证 Content-Type 与 OGG/MP3 文件头；成功后按 URL 与内容 SHA-256 原子落盘并以 64 MiB LRU 清理。OGG 使用原版 `OggAudioStream`，MP3 使用仅客户端 JLayer 1.0.1（LGPL-2.1、独立 Jar-in-Jar、不重定位；完整许可证与 NOTICE 已随源树保存）解码 16-bit PCM，再经 Forge 的 `SoundInstance#getStream` 公开扩展点交给原版 SoundEngine。不得把这批静态实现写为安全或播放验收通过；真实 HTTPS、OGG/MP3、缓存、失败和新登录不补播探针仍须在 Hosted Runner 完成。

第四批静态加固追加：客户端把 IPv4-mapped、IPv4-compatible、标准 NAT64 嵌入 IPv4 与 IPv6 文档/保留前缀统一按真实目标拒绝；服务端对数字/点主机名直接拒绝而不做 DNS。每一跳 DNS、TCP、TLS、重定向和响应体共享一个 10 秒截止点，且客户端直接向已验证 IP 的 TLS socket 写固定 GET（无 URLConnection 回退、Cookie、认证或代理）；TLS socket 到期会主动关闭。缓存只信任普通非链接文件，并复算大小、魔数和 SHA-256；同 URL 合并单次下载、过期 `.part` 清理，避免并发覆盖与残留。OGG/MP3 都在客户端工作线程完整解码为有 128 MiB 上限的 PCM 后才交给原版音频线程，因此解码失败仍会回到客户端失败提示；同时至多两条远程音频占用解码/播放槽位，断线/换服通过连接代次忽略旧下载。JLayer 的 LGPL 与 NOTICE 已配置进入正式 Jar，并由质量门禁检查。任意门配对/传送和八音盒编辑/播放都同时重验 `Player.mayBuild()` 与 `Level.mayInteract` 的位置权限；任意门还要求双方各**恰好一个**安全落点，新增第二安全点即拒绝并清理双向关系。质量门禁拒绝不存在的带位置 `mayBuild` 调用，防止该 Forge 1.20.1 API 错误回归。为后续真实在线验收新增自制 0.25 秒正弦波 OGG/MP3 和截断失败夹具，只放 `src/ciTest/resources/ci-audio/`，有生成脚本、摘要与来源说明，正式 Jar 门禁明确排除它们。上述均仍是静态实现，不能替代 Hosted Runner 的真实验收。

第四批真实验收探针已追加但尚未在 Hosted Runner 得出结论：Alice 必须站在服务端布置的方块上，通过真实右键打开生产 `MusicBoxScreen`，分别提交 OGG、MP3、截断 OGG 三次 URL；两客户端都只在生产 `PlaySoundEvent` 进入原版音频引擎且其真实 `AudioStream.read()` 返回非空 PCM 后写一次原子 marker。服务端逐字段交叉核验两端同一事件 UUID、URL、声源、观察者、格式和缓存命中。初次 OGG 后工作流仅将 `raw.githubusercontent.com` 解析为回环地址；第二次相同 URL 若未先命中 SHA 缓存即被生产公网策略拒绝，故不能伪造为缓存成功。恢复公网解析后再验证 MP3，截断 OGG 必须先收到真实 S2C 再得到客户端失败事件且不得产生 PCM marker；最后真实踢出 Bob、经 `ConnectScreen` 重连后先连续 80 tick、再额外 40 tick 宽限不得收到历史播放事件。这里验证的是 **120 tick 有限观察窗口**，不能虚报为无限期数学证明。临时阶段旗标只安排顺序，不是成功 marker；所有结果文件由客户端或服务端事实生成，不能预写。
