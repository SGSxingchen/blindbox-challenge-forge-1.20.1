# P1 全量运行验收矩阵

> 目标：把 P1 从“可构建、专服可启动”提升为真实客户端、双客户端并发和生命周期恢复均有机器断言。动态 Forge/Gradle/Minecraft 测试只允许在 GitHub Hosted Runner 执行。

## 必需工作流

1. `single-client.yml`：Xvfb + Mesa 启动真实 Forge 1.20.1 Client，进入主菜单并连接本次 Jar 的专服；正常退出，客户端与服务端无 crash-report、FATAL、Mixin/客户端类泄漏。
2. `multi-client.yml`：两个独立游戏目录、独立玩家身份的真实客户端连接同一专服，执行 P1 业务探针。
3. `lifecycle-recovery.yml`：固定测试世界与玩家 UUID，执行 `save-all flush` 后 SIGKILL(-9)，用同世界重启并核对资产与事务状态。
4. `regression-report.yml`：由五条必需工作流完成事件触发，按同一 HEAD 等待并核验质量、专服、强杀恢复、单客户端和双客户端；任何 failed/cancelled/skipped/missing 均失败，并上传机器可读汇总 JSON。

## P1 业务断言

|场景|前置与操作|必须断言|
|---|---|---|
|空奖池开盒|玩家持有唯一 token 的盲盒，奖池为空，长按完成|盲盒不消耗；库存不变；奖池仍为 0；服务端存活|
|正常打包|选择多个不同槽位、不同数量且带唯一名称/NBT 的物品|精确扣除；只增加 1 个完整 bundle；只交付 1 个唯一 token 盲盒；总资产守恒|
|伪造打包包|错误容器 ID、越界槽位、重复槽位、过量数量、过期指纹、禁打包物、关闭菜单后提交|全部拒绝；库存/奖池不变；无断线和服务端异常|
|菜单关闭|打开打包菜单后关闭，不提交|无物品临时转移；库存与奖池不变|
|满背包打包|没有空间接收盲盒|事务不开始或完整回滚；被选物品不扣；奖池不增加|
|正常开盒|奖池含唯一 bundle，背包可容纳|恰好消耗 1 个 token；恰好删除被选 bundle；完整 NBT 奖品只交付一次|
|满背包开盒|bundle 无法完整容纳|盲盒不消耗；奖池条目不删除；无地面公共掉落；资产守恒|
|中止开盒|长按期间松手、换手、死亡、掉线|不开盒；不消耗 token；减速修饰器清理；重连后无残留使用态|
|两人竞争一条奖池|两个客户端近同时完成开盒，奖池仅 1 条|最多 1 人成功；另一人保留盲盒；bundle 只交付一次；奖池为 0|
|并发打包|两个客户端分别打包唯一 NBT 资产|产生 2 条不同 UUID bundle 和 2 个不同 token；不串玩家、不覆盖版本|
|断线重连|PREPARED/COMMITTED 边界附近断线并重连|不会重复扣除/补发；事务只归属原玩家；状态可审计|
|正常保存重启|打包和开盒后 `save-all flush`、正常 stop、重启|奖池、事务、玩家库存与 token UUID 保持一致|
|强杀恢复|在预设阶段写 marker，`save-all flush` 后 SIGKILL(-9)，同世界/同 UUID 重启|逐槽物品、奖池条目、事务阶段、token 与 payload 总量可核对；无复制/吞失；需要隔离时必须稳定进入 `MANUAL_REVIEW` 且 payload 完整|
|错误方向/突发包|发送错误方向、未知容器、无权限命令、有限突发请求|服务端拒绝且存活；无越权、无状态变化、无包洪水失控|

## 资产守恒口径

测试资产必须含唯一名称、耐久、附魔和自定义 NBT。每个检查点生成 canonical JSON：玩家逐槽物品、光标栈、奖池 bundle、事务 payload、地面归属掉落、token UUID。按物品 ID + 数量 + 规范化 NBT 汇总，事务隔离中的 payload 计入“受控资产”，但同一资产不得同时出现在玩家库存和可领取奖池中。

## 禁止伪通过

- 不得用纯单元测试、编译成功、客户端日志启动前半段或伪造 marker 代替真实 Forge Client/Server。
- 不得 `continue-on-error`、删除失败场景、吞异常、降低部署组合或把人工步骤写成自动覆盖。
- 测试探针只能注入 CI 临时产物，不得进入正式发布 Jar。
- 未执行的掉电窗口必须列为未覆盖，不得从 `save-all flush` 后强杀结果外推。

## 当前状态（2026-08-06）

- 质量构建：已通过，run `31052228486`。
- 真实专服启动、命令、`save-all flush`、正常停止：已通过，run `31052228529`。
- 已新增 `lifecycle-recovery.yml`：加载正式 Jar 与隔离 `ciTest` 探针，真实启动服务端、导出 canonical JSON、执行 `save-all flush` 后 SIGKILL(-9)，再以同一世界重启并比较玩家/奖池/事务/保留状态。当前里程碑仅覆盖空世界恢复基础设施；PACK/OPEN 阶段注入与唯一 NBT 资产守恒仍未覆盖，不得外推为完整事务恢复通过。
- 真实 Xvfb 单客户端标题界面启动已通过，run `31063024188`。
- 已新增 `multi-client.yml`：两个独立游戏目录与玩家身份连接同一真实专服，服务端 canonical 必须同时观测两个不同 UUID/名称和完整 36 槽库存结构，随后客户端正常断线、服务端 `save-all flush` 并停止。该里程碑只覆盖真实双客户端联机和身份隔离；并发打包/开盒、竞争最后 bundle、重连恢复与网络负例仍不得标为已覆盖。


## 2026-08-06 强杀恢复增量

`lifecycle-recovery.yml` 已从空世界基础链扩展为唯一 NBT 的 PACK/OPEN SavedData 证据夹具：两个 bundle、PACK/OPEN 各一条未完成 schema v2 事务及一条 OPEN reservation 在 `save-all flush → SIGKILL(-9) → 同世界重启` 前后必须 canonical 完全一致。该里程碑验证持久奖品 payload、收据与竞争保留不复制不丢失；在线玩家逐槽变化与登录恢复仍由后续真实客户端矩阵覆盖。
## 2026-08-06 取消与网络负例增量

双客户端业务探针新增真实 ServerPlayer 断言：菜单关闭后的重放、伪造容器编号、伪造会话 nonce 均拒绝且不改资产；盲盒使用状态与减速修饰在统一取消路径中同时清理。提交 `272f97f` 的质量、专服、强杀恢复、真实单客户端和真实双客户端五条 Actions 已全部通过，其中双客户端 run 为 `31065871174`。

同一打包选择以相同旧 NBT 指纹重复进入服务端业务层时，第二次必须拒绝；奖池保持 1 条、PACK 事务保持 1 条、玩家只持有 1 个盲盒 token。该增量已由提交 `e69fb93` 的五条 Hosted Runner 门禁验证通过。

换手/松开现通过原版 `releaseUsing` 路径验证，死亡清理通过 Forge `LivingDeathEvent` 生产事件入口验证；两者均要求盲盒使用态、玩家原版使用态和减速修饰同步清理。奖池耗尽后的同一盲盒重复开盒请求必须持续失败，且不得新增事务、消耗 token 或改变奖池。该增量由提交 `849b88a` 之后的新一轮五条 Actions 验证。

## 2026-08-06 真实断线重连增量

双客户端流程新增真实断线重连：Alice 持唯一 token 盲盒进入长按状态后，由专服 `kick` 触发真实登出事件；客户端在断线界面自动重新连接同一专服。重连稳定 40 tick 后，服务端断言该 token 未消耗或丢失，玩家无残留使用态与减速修饰，并上传重连 marker、canonical JSON 和双端日志。此项已由提交 `d5ff391` 的真实双客户端 run `31067326341` 验证通过；同提交的质量、专服、强杀恢复和真实单客户端也全部成功。

## 2026-08-06 强制汇总门禁

新增 `regression-report.yml` 与 `.github/ci/check_required_workflows.py`：针对同一提交 SHA 等待五条必需工作流，并将缺失、失败、取消或跳过统一判为失败；成功时上传 `p1-required-workflows.json`，禁止只看部分绿灯就宣称 P1 全绿。
